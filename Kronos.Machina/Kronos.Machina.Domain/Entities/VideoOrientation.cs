namespace Kronos.Machina.Domain.Entities
{
    /// <summary>
    /// Now redundant, but will become really needed when
    /// short (reel) video format will be added to 
    /// the videohosting.
    /// </summary>
    public enum VideoOrientation
    {
        Horizontal, 
        Vertical,
        Unidentified
    }
}
